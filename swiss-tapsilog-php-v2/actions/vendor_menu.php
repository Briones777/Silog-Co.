<?php require_once __DIR__.'/../includes/auth.php'; require_vendor(); verify_csrf();$id=(int)($_POST['id']??0);$action=$_POST['action']??'';if($action==='toggle'&&$id>0){$pdo->prepare('UPDATE menu_items SET available=1-available WHERE id=?')->execute([$id]);flash('Menu availability updated.');}else{flash('Invalid menu action.','error');}header('Location: ../vendor.php?tab=menu');exit;
